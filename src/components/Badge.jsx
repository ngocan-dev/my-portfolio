const Badge = ({ children }) => (
  <span className="inline-flex rounded-full border border-slate-700 bg-slate-800 px-3 py-1 text-sm text-slate-200">
    {children}
  </span>
);

export default Badge;
